# TODO / Backlog

## Known Issues (`fix`)

- [ ] **fix(tilt):** Prevent layout scrollbar overflow when rotating large host elements via 3D transforms.
- [ ] **fix(tilt/glass/pixie):** Preserve backdrop-filter blur and layer ordering for particle canvas when tilt 3D transform is active.
- [ ] **fix(glass):** Restore backdrop filter effects (saturation, chroma, blur) lost during 3D transform execution.
- [ ] **fix(ripple):** Fix pixelation issue on large objects.
- [ ] **fix(color):** Add support for `oklch` color format.
- [ ] **fix(pixie):** Fix canvas element becoming invisible when host or parent element uses `backdrop-filter` (e.g. glass effect).

## Planned Features (`feat`)

- [ ] **feat(tilt):** Support negative/sub-unit scale values in `--fx-tilt-scale` (allow element to shrink on hover).
- [ ] **feat(tilt):** Add subtle breathing oscillation to hover scale.
- [ ] **feat(docs):** Add visual demonstration (screenshot/GIF/video) to README.
